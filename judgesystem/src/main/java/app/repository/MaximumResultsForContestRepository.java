package app.repository;

import app.domain.entity.MaximumResultsForContest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MaximumResultsForContestRepository extends JpaRepository<MaximumResultsForContest, Long> {
}
